const menuBtn=document.querySelector('.menu-btn'),nav=document.querySelector('.nav');if(menuBtn)menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));
const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const topBtn=document.querySelector('.back-top');window.addEventListener('scroll',()=>{if(window.scrollY>500)topBtn?.classList.add('show');else topBtn?.classList.remove('show')});topBtn?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
document.querySelectorAll('[data-slider]').forEach(slider=>{const track=slider.querySelector('.slider-track');const prev=slider.querySelector('.prev');const next=slider.querySelector('.next');let index=0;function maxIndex(){const cards=track.querySelectorAll('.slider-card').length;const visible=window.innerWidth<700?1:window.innerWidth<1100?2:3;return Math.max(0,cards-visible)}function update(){const card=track.querySelector('.slider-card');if(!card)return;const gap=22;const w=card.getBoundingClientRect().width+gap;track.style.transform=`translateX(${-index*w}px)`;}prev?.addEventListener('click',()=>{index=Math.max(0,index-1);update()});next?.addEventListener('click',()=>{index=Math.min(maxIndex(),index+1);update()});window.addEventListener('resize',()=>{index=Math.min(index,maxIndex());update()});update();});
